# Українські Інтернет-Магазини Скрапер

Об'єднаний скрапер для збору інформації про товари з українських інтернет-магазинів з підтримкою SQL бази даних, Excel експорту та детального логування.

## 📋 Підтримувані Сайти

- **Fora.ua** - категорія "Молочні продукти та яйця"
- **Novus/Zakaz.ua** - категорія "Молочні продукти та яйця"
- 🔄 Готовий до розширення для інших сайтів

## ✨ Особливості

- **🔄 Інкрементальне оновлення** - тільки нові та змінені товари
- **📊 Подвійне зберігання** - SQLite база даних + Excel експорт
- **📈 Детальний прогрес** - інкрементальне логування в txt файл
- **🛡️ Стійкість до помилок** - автоматичні повтори та відновлення
- **⚙️ Конфігурація через JSON** - легке налаштування
- **🔀 Ротація User-Agent** - уникнення блокування
- **💾 Резервні копії** - автоматичне створення backup БД
- **🎯 Селективний скрапінг** - можливість скрапити окремі сайти

## 🚀 Швидкий Старт

### 1. Встановлення залежностей

```bash
pip install -r requirements.txt
```

### 2. Запуск скрапера

#### Скрапити всі сайти:
```bash
python unified_scraper.py --max-pages 5
```

#### Скрапити тільки Fora.ua:
```bash
python unified_scraper.py --site fora --max-pages 3
```

#### Скрапити тільки Novus:
```bash
python unified_scraper.py --site novus --max-pages 10
```

#### Тільки експорт існуючих даних:
```bash
python unified_scraper.py --export-only
```

### 3. Налаштування конфігурації

Перший запуск автоматично створить `config.json`. Можна редагувати для зміни параметрів:

```json
{
  "sites": {
    "fora": {
      "enabled": true,
      "max_pages": 5,
      "delay": 0.2
    },
    "novus": {
      "enabled": true, 
      "max_pages": 10,
      "delay": 0.1
    }
  },
  "database": {
    "path": "data/unified_products.db",
    "backup_enabled": true
  },
  "scraping": {
    "user_agent_rotation": true,
    "session_renewal_interval": 50
  }
}
```

## 📁 Структура Файлів

```
├── unified_scraper.py     # Головний скрапер
├── config.json           # Конфігурація
├── requirements.txt      # Залежності
├── data/
│   ├── unified_products.db     # SQLite база даних
│   ├── unified_products.xlsx   # Excel файл
│   ├── unified_progress.txt    # Лог прогресу
│   ├── scraper.log            # Детальний лог
│   └── *.backup_*             # Резервні копії БД
└── legacy/
    ├── scraper.py        # Старий Fora скрапер
    └── novus_scraper.py  # Старий Novus скрапер
```

## 🗃️ Схема Бази Даних

### Таблиця `products`
```sql
CREATE TABLE products (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    site_name TEXT NOT NULL,          -- Назва сайту
    title TEXT,                       -- Коротка назва
    full_name TEXT,                   -- Повна назва
    price REAL,                       -- Ціна
    currency TEXT,                    -- Валюта (₴, грн)
    description TEXT,                 -- Опис товару
    image_url TEXT,                   -- URL зображення
    url TEXT NOT NULL UNIQUE,         -- URL товару (унікальний ключ)
    first_seen TIMESTAMP,             -- Перша поява
    last_updated TIMESTAMP,           -- Останнє оновлення
    is_active INTEGER DEFAULT 1       -- Активний товар
);
```

## 📊 Excel Експорт

Автоматично створюються наступні листи:
- **"Всі товари"** - зведена таблиця всіх товарів
- **"Fora.ua"** - товари тільки з Fora
- **"Novus/Zakaz.ua"** - товари тільки з Novus

## 📈 Прогрес Логування

### Приклад `data/unified_progress.txt`:
```
2025-11-03 22:30:15 - === Початок скрапінгу ===
2025-11-03 22:30:16 - Початок скрапінгу сайту: Fora.ua
2025-11-03 22:30:20 - Fora.ua: Сторінка 1 - знайдено 24 товарів
2025-11-03 22:30:25 - Fora.ua: Оброблено товар 'Молоко 2.5%' (https://fora.ua/...)
2025-11-03 22:35:45 - Завершено Fora.ua: 3 сторінок, 72 товари, 2 помилки. Тривалість: 0:05:29
2025-11-03 22:35:46 - === Скрапінг завершено за 0:05:31 ===
2025-11-03 22:35:46 - Total products: 72, Total errors: 2
```

## ⚙️ Параметри Командного Рядка

```bash
python unified_scraper.py [OPTIONS]

Опції:
  --config CONFIG       Шлях до файлу конфігурації (за замовчуванням: config.json)
  --site {fora,novus,all}  Який сайт скрапити (за замовчуванням: all)
  --max-pages N         Максимальна кількість сторінок для скрапінгу
  --export-only         Тільки експортувати існуючі дані в Excel
  -h, --help           Показати це повідомлення та вийти
```

## 🔧 Вирішення Проблем

### Помилки мережі
- Скрапер автоматично повторює запити до 5 разів
- Ротація User-Agent кожні 10 запитів
- Оновлення сесії кожні 50 запитів

### Блокування сайтами
- Налаштовані реалістичні затримки між запитами
- Різні User-Agent для імітації браузерів
- Можна збільшити `delay` у конфігурації

### Пошкоджені дані
- Автоматичні резервні копії БД перед кожним запуском
- Індекси в БД для швидкого пошуку дублікатів
- Валідація даних перед збереженням

### Відновлення після збоїв
- Прогрес зберігається в реальному часі
- Інкрементальне оновлення - не втрачаються попередні дані
- Детальні логи для діагностики

## 📝 Приклади Використання

### Щоденне оновлення каталогу:
```bash
# Ранкове оновлення з усіх сайтів
python unified_scraper.py --max-pages 5
```

### Швидка перевірка нових товарів Novus:
```bash
# Тільки перші 3 сторінки Novus
python unified_scraper.py --site novus --max-pages 3
```

### Експорт без скрапінгу:
```bash
# Оновити Excel файл з існуючих даних БД
python unified_scraper.py --export-only
```

### Кастомна конфігурація:
```bash
# Використати власний конфіг
python unified_scraper.py --config my_config.json --max-pages 10
```

## 🆚 Порівняння з Попередніми Версіями

| Особливість | Старі скрапери | Unified Scraper |
|-------------|----------------|------------------|
| **Бази даних** | Окремі | ✅ Єдина консолідована |
| **Конфігурація** | Хардкод | ✅ JSON конфіг |
| **Логування** | Базове | ✅ Детальне + прогрес |
| **Відновлення** | Ні | ✅ Автоматичне |
| **Backup** | Ні | ✅ Автоматичні копії |
| **Excel експорт** | Окремі файли | ✅ Мульти-лист файл |
| **User-Agent** | Фіксований | ✅ Ротація |
| **Розширюваність** | Складна | ✅ Модульна архітектура |

## 🚧 Плани Розвитку

- [ ] Додати підтримку ATB Market
- [ ] Веб-інтерфейс для моніторингу
- [ ] API для доступу до даних
- [ ] Система сповіщень про зміни цін
- [ ] Docker контейнеризація
- [ ] Планувальник задач (cron інтеграція)

## 📄 Ліцензія

МIT License - використовуйте вільно для персональних та комерційних проектів.